# Sprint S03: Agent Workflow And Project Integration

- Status: `DONE`
- Dependencies: S02.

## Objective

Make journaling an explicit long-lived-plan workflow while keeping CLI,
documentation, installation, generated copies, and project auditing aligned.

## Questions

No sprint-specific questions are currently open.

## Feature F01: Plan-Execution Workflow

Acceptance: the canonical reusable skill tells top-level agents and subagents
when and how to journal, how to preserve continuous timing, how to end replies,
and how to continue while reporting failures.

### [DONE] Tasklet S03-F01-T01: Define Agent Journaling Rules

Update the canonical plan-execution skill with action boundaries, CLI identity
arguments, subagent behavior, command wrapping, heartbeat semantics, `over`,
and non-blocking failure reporting.

Result: the canonical skill now defines first-action timing, transitions,
quoted commands, agent hierarchy, subagent completion, `over`, timestamp use,
and mandatory non-blocking failure reporting.

### [DONE] Tasklet S03-F01-T02: Regenerate Host Copies

Run the owning generator and verify that published host adapters match the
canonical skill.

Result: regenerated the OpenClaw copy from the canonical skill and installed
the updated skill into Codex with `install-to-codex.sh`.

## Feature F02: Project Integration

Acceptance: users can install and discover the CLI and database contract, and
Ponytail's structure and audit commands recognize every new owned artifact.

### [DONE] Tasklet S03-F02-T01: Integrate CLI Installation

Ensure `install-cli.sh` installs `project_journal.sh` through the existing
automatic CLI discovery path and add focused installer coverage only where
the existing generic proof is insufficient.

Result: generic CLI discovery installs the new command; the final source was
installed and verified in `~/.local/bin` while Codex installation was checked.

### [DONE] Tasklet S03-F02-T02: Synchronize Structure And Documentation

Update canonical project structure, command inventories, configuration
ownership, setup instructions, and user documentation without duplicating the
journal contract.

Result: synchronized project ownership, commands, package distribution, setup,
configuration defaults, authentication guidance, and user-facing CLI docs.

### [DONE] Tasklet S03-F02-T03: Extend Project Audit

Teach `audit_pm.sh` to flag missing or invalid project journal configuration
when applicable without treating ignored telemetry as a PM artifact.

Result: `audit_pm.sh` now requires the root configuration and delegates exact
V1 validation to the journal CLI's single canonical reader.

### [DONE] Tasklet S03-F02-T04: Prove Integrated Workflow

Add focused regression coverage for skill requirements, distribution,
installation, configuration ownership, and audit behavior.

Validation: CLI, cadence, OpenClaw-copy, installation, and distribution tests
passed. The first distribution attempt lacked `npm`; the configured `pnpm dlx
--package npm@11.6.2` runtime supplied it and the focused distribution test
then passed without product changes.

## Sprint Validation

Focused integration, generated-copy, installer, and distribution checks passed.
