# Sprint S04: Final Acceptance And Reconciliation

- Status: `DONE`
- Dependencies: S01, S02, and S03.

## Objective

Validate the complete committed journal workflow once against the final tree
and reconcile every durable management record.

## Questions

No sprint-specific questions are currently open.

## Feature F01: Final Acceptance

Acceptance: the complete final tree passes configured acceptance and the plan
contains sufficient evidence to close without relying on chat history.

### [DONE] Tasklet S04-F01-T01: Run Core Acceptance

Run the configured full Node test command once after final relevant edits,
followed by the rule-copy and version checks.

Validation: the focused integration selection passed 118 tests. The configured
full `npm test` command passed 217 core tests, 23 Pi extension tests, and four
MCP tests. The explicit rule-copy check found all eight copies synchronized,
and the version check found all seven version files pinned at `4.8.4`.

### [DONE] Tasklet S04-F01-T02: Validate PostgreSQL Workflow

Run the complete PostgreSQL installation, repeatability, role isolation,
transition, heartbeat, recovery, and sensitive-command acceptance against a
supported PostgreSQL 18 instance.

Validation: PostgreSQL 18 accepted two consecutive setup runs, and the
transactional contract suite passed reporter isolation, analyst visibility,
raw-mutation denial, and controlled action creation. Live CLI evidence also
proved foreground heartbeats, heartbeat-loss recovery, tasklet sequencing,
quoted-command redaction, command-to-waiting transitions, and shared-prompt
subagent completion.

### [DONE] Tasklet S04-F01-T03: Reconcile Plan Evidence

Inspect the complete project change-set, reconcile every tasklet and sprint,
record final validation evidence, and mark the plan complete only after all
owned checks pass.

Result: every sprint and tasklet is reconciled against the final tree. The
journal contains the final S04 command actions and exactly one open top-level
waiting action, which `over` closes when this reply returns control.

## Sprint Validation

All configured acceptance and live PostgreSQL workflow checks passed.
