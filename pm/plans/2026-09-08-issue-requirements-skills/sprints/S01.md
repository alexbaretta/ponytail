# S01: Define and publish management skills

Status: IN_PROGRESS

Questions: [RESOLVED] See the manifest for scope and authority.

Atomicity review: complete. Each tasklet owns one skill definition or its
necessary integration/publication/validation unit. Ordered dependencies cover
all shared policy and generated artifacts.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": [
      "skills",
      "tests",
      "registry.tsv",
      "generated",
      ".openclaw",
      "plugin.yaml",
      "AGENTS.md",
      "PROJECT_STRUCTURE.md",
      "README.md",
      "pm/plans"
    ]
  },
  "execution": {
    "status": "IN_PROGRESS",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Configurable management documents

Acceptance: one shared lifecycle owner; requirements reconciliation at active
entry; canonical issue identity and epic links; status-directory plan placement;
all publication surfaces synchronized.

### [DONE] Tasklet S01-F01-T01: Define issue tracking

Exact paths are frozen in the sibling graph. Implement the corresponding
manifest contract; preserve existing execution/approval and metadata rules.
Validate skill syntax, linked owners, and focused existing policy/publication
checks. Final tasklet owns the full repository acceptance and lifecycle record.

### [DONE] Tasklet S01-F01-T02: Define requirements

Exact paths are frozen in the sibling graph. Implement the corresponding
manifest contract; preserve existing execution/approval and metadata rules.
Validate skill syntax, linked owners, and focused existing policy/publication
checks. Final tasklet owns the full repository acceptance and lifecycle record.

### [DONE] Tasklet S01-F01-T03: Integrate plan and structure policy

Exact paths are frozen in the sibling graph. Implement the corresponding
manifest contract; preserve existing execution/approval and metadata rules.
Validate skill syntax, linked owners, and focused existing policy/publication
checks. Final tasklet owns the full repository acceptance and lifecycle record.

### [DONE] Tasklet S01-F01-T04: Publish and document

Exact paths are frozen in the sibling graph. Implement the corresponding
manifest contract; preserve existing execution/approval and metadata rules.
Validate skill syntax, linked owners, and focused existing policy/publication
checks. Final tasklet owns the full repository acceptance and lifecycle record.

### [ ] Tasklet S01-F01-T05: Validate and reconcile

Exact paths are frozen in the sibling graph. Implement the corresponding
manifest contract; preserve existing execution/approval and metadata rules.
Validate skill syntax, linked owners, and focused existing policy/publication
checks. Final tasklet owns the full repository acceptance and lifecycle record.

## Implementation evidence

- Canonical skill definitions, host configuration references, registry, runtime
  metadata, Hermes manifest, and OpenClaw copies are synchronized.
- Thirty focused policy/publication tests passed. Skill Creator validation
  passed for both new skills and plan-execution. YAML metadata and relative
  links passed for all four changed skills; `git diff --check` passed.
- Build impact: no affected or indeterminate targets after installing the
  missing root dependencies. Installation's existing prepare hook built TSTS.
- Manual contract review: default and custom issue types share one status
  owner with plans; custom roles preserve activation gates; FEAT additions,
  documented BUG no-edit reconciliation, TASK no-impact review, reopen/resume,
  epic links, whole-plan moves, and immutable sprint states are covered.
- The bundled MCP dependency update encountered npm's null `matches` error
  while inspecting existing pnpm links. Its existing four tests pass; no
  tracked dependency files changed. Full repository acceptance remains pending.
- Legacy PM CLI behavior is documented explicitly; this skill-definition
  change does not advertise CLI support for the new layout.
