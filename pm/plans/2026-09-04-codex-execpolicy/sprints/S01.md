# S01: Implement the V1 Codex Execpolicy Pipeline

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["cli", "config", ".ponytail", "skills/codex-execpolicy", "scripts", "tests", "docs", "generated", "README.md", "PROJECT_STRUCTURE.md", "registry.tsv", "versioned-data-contracts.json", "package.json", ".openclaw/skills/codex-execpolicy", "pm/plans/2026-09-04-codex-execpolicy"]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

- Status: `DONE`
- Objective: deliver a reconstructible, confirmation-gated policy pipeline.
- Questions: none. The user approved the global per-user aggregation effect
  after explicitly identifying its cross-agent denial-of-service risk.

## Feature S01-F01: Versioned policy pipeline

### [DONE] Tasklet S01-F01-T01: Define policy ownership and contracts

- Add the Ponytail baseline, project proposal location, accepted-state contract,
  reusable skill, registry entry, and contract/structure ownership records.
- Project directives remain proposals regardless of executable location.
- Focused proof: registry, contract manifest, and skill validation checks.

### [DONE] Tasklet S01-F01-T02: Implement policy synthesis

- Replace the non-executing condenser with a parse-safe CLI that imports current
  Codex rules once, registers projects, computes a provenance-rich proposal,
  confirms changes, writes accepted state, and installs generated rules.
- Support isolated home/Codex roots, dry-run, check, restore, proposal-digest
  acceptance, and deterministic output.
- Focused proof: `node --test tests/condense-codex-rules.test.js`.

### [DONE] Tasklet S01-F01-T03: Integrate installation and documentation

- Run the full policy pipeline from the combined installer, synchronize CLI and
  distribution tests, generate the published skill copy, and document the
  trust and recovery model.
- Focused proof: installer, distribution, registry, and OpenClaw tests.

### [DONE] Tasklet S01-F01-T04: Repair portable journal locking

- Replace the Bash-4-only lock-owner PID and sandbox-dependent heartbeat
  recognition with a portable PID and action-specific heartbeat lease.
- Focused proof: the detached-heartbeat and stale-lock test in
  `tests/cli-tools.test.js`.

### [DONE] Tasklet S01-F01-T05: Validate and reconcile the plan

- Run full repository acceptance after final relevant edits, record evidence,
  and reconcile plan and sprint state without installing into the live home.

## Validation Evidence

- The 6 focused execpolicy tests passed both with isolated parser validation
  and through the installed Codex execpolicy parser.
- CLI/installer, registry, distribution, generated skill, contract, and
  project-journal focused checks passed.
- The portable journal heartbeat regression passed repeatedly without process
  inspection privileges.
- Final `npm test` passed 259 core, 23 Pi extension, and 4 MCP tests.
- Version, shell syntax, generated output, rule-copy, and diff checks passed.
- Acceptance used isolated home directories; the live user policy was not
  installed or changed.
