# S01: Blessed worktree configuration

Status: DONE

Questions: [RESOLVED] Configuration lives at
`.agents/config/ponytail.json`; blessing stores a worktree path only; the
`-worktree` suffix is optional for both commands; no compatibility reader is
required.

Atomicity review: complete. The CLI, QA reader, fixtures, and configuration
move share one contract and are serialized in one implementation batch.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["cli", "scripts", "tests", "docs", "skills"]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Select tracked project configuration by worktree

Status: DONE

### [DONE] Tasklet S01-F01-T01: Replace global registry contract

Store exact repository entries containing a main root and nullable blessed
worktree. Remove the mistaken global component map without compatibility.

### [DONE] Tasklet S01-F01-T02: Move project configuration into `.agents`

Move the strict project V1 file, represent components with explicit aliases,
and keep component mutations atomic within the invoking worktree.

### [DONE] Tasklet S01-F01-T03: Add blessing commands

Implement both optional-suffix forms, validate repository membership and a
clean tracked project configuration, and print the selected root exactly.

### [DONE] Tasklet S01-F01-T04: Read foreign identities from blessed worktrees

Make QA reject missing or invalid blessings and use only each foreign
repository's selected worktree configuration.

### [DONE] Tasklet S01-F01-T05: Synchronize tests, docs, and generated guidance

Add focused worktree and alias proof, update repository configuration and
documentation, and regenerate project-structure guidance. The generator also
synchronizes the stale OpenClaw Ponytail copy with the user's preceding
canonical policy commit.

### [DONE] Tasklet S01-F01-T06: Complete final acceptance

Run build impact and the repository final acceptance commands, migrate the
live Ponytail registry and tracked configuration, reconcile the plan, and
commit the complete change-set.

## Implementation evidence

- Focused CLI, project-validation, and OpenClaw generation tests passed 127
  tests with no failures.
- The deleted `.ponytail/project.json` was queried before deletion and was not
  a TSTS input. The final build-impact query reported no affected or
  indeterminate targets for the remaining paths.
- Worktree fixtures prove explicit blessing, both optional-suffix command
  forms, dirty-config rejection, and foreign identity selection.
- Final acceptance exposed and repaired the TSTS installer fixture's missing
  commit step for the newly tracked project configuration.
- Implementation commit `059cb33` established the new contract. Fixture
  correction commit `7a1ebe2` preserved the registration precondition in the
  installed TSTS test.
- Final build impact reported no affected or indeterminate targets. `npm test`
  passed, including 298 core, 23 Pi extension, 4 MCP, and 58 TSTS tests.
  Rule-copy validation matched 8 copies, and all 7 versions remain at 4.8.4.
- The live strict V1 registry blesses the Ponytail checkout; the two registered
  downstream repositories remain unblessed until their tracked project
  configurations are migrated there.
