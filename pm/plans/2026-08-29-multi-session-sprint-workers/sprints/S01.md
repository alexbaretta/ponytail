# S01: Implement Multi-Session Sprint Workers

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["skills/plan-execution", "tests", ".openclaw/skills/plan-execution", "pm/plans/2026-08-29-multi-session-sprint-workers"]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

- Status: `IN_PROGRESS`
- Objective: make isolated secondary Codex root tasks the preferred execution
  tier for dependency-ready sprints without weakening V3 packet leases or
  canonical master ownership.
- Questions: none; the user explicitly approved implementation and install.

## Feature S01-F01: Multi-session sprint execution

### [DONE] Tasklet S01-F01-T01: Define secondary root orchestration

- Amend the canonical policy and focused conformance test to distinguish
  secondary Codex tasks from same-session subagents; require persistent sprint
  affinity, isolated worktrees, master-issued immutable packet leases,
  secondary-root Git ownership, nested subagent boundaries, and master-owned
  follow-up waves and acceptance.
- Preserve selector formats and semantics; a secondary task receives no
  autonomous whole-sprint authority without an explicit future selector
  contract.
- Focused proof: `node --test tests/plan-execution-subagents.test.js`.
- Result: passed 4 tests, including the multi-session ownership contract.

### [DONE] Tasklet S01-F01-T02: Synchronize generated skill copies

- Regenerate the OpenClaw `plan-execution` skill from the canonical source.
- Focused proof: `node scripts/build-openclaw-skills.js` and
  `node --test tests/openclaw-skills.test.js`.
- Result: generation changed only the owned `plan-execution` copy; all 92
  OpenClaw skill checks passed.

### [DONE] Tasklet S01-F01-T03: Validate and install Ponytail

- Run focused policy and generated-copy checks, full repository acceptance,
  rule-copy and version checks, and `./scripts/install.sh`.
- Verify the installed Codex `plan-execution` skill matches the canonical
  source byte-for-byte.
- No product build applies.
- Result: complete repository acceptance and installation passed; the
  installed skill matched the canonical source.

## Validation Evidence

- `node --test tests/plan-execution-subagents.test.js`: 4 passed.
- `node scripts/build-openclaw-skills.js`: generated canonical registry
  outputs; only `.openclaw/skills/plan-execution/SKILL.md` changed.
- `node --test tests/openclaw-skills.test.js`: 92 passed.
- `npm test`: 249 core tests, installer checks, 23 Pi tests, and 4 MCP tests
  passed.
- `node scripts/check-rule-copies.js`: all 8 rule copies matched.
- `node scripts/check-versions.js`: all 7 version files remained at `4.8.4`.
- Build: not applicable; Ponytail declares no build target.
- `./scripts/install.sh`: completed.
- Installed verification: canonical and installed Codex `plan-execution`
  skill files matched byte-for-byte.
- Completed: `2026-08-29T02:49:20-07:00`.
