# S01: Implement Dependency-Parallel Execution

<!-- ponytail-plan-sprint
{
  "schemaVersion": 2,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["skills/plan-execution", "tests", "versioned-data-contracts.json", ".openclaw/skills/plan-execution", "pm/plans/2026-08-26-dependency-parallel-execution"]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

- Status: `DONE`
- Objective: introduce and install the immutable V3 orchestration contract.
- Questions: none; the user selected maximum safe parallelization.

## Feature S01-F01: V3 dependency-parallel orchestration

### [DONE] Tasklet S01-F01-T01: Select dependency-ready V3 sprints

- Add immutable V3 sprint metadata reading, dependency-only execution-state
  validation, tasklet-derived cross-sprint path validation, and parallel V3
  readiness while preserving V1/V2 behavior.
- Focused proof: `node --test tests/ready-sprints.test.js`.

### [DONE] Tasklet S01-F01-T02: Select ordered V3 tasklet packets

- Add immutable V3 tasklet metadata reading and deterministic bounded packet
  selection while preserving V1 scalar and V2 wave outputs.
- Focused proof: `node --test tests/ready-tasklets.test.js`.

### [DONE] Tasklet S01-F01-T03: Amend the orchestration policy

- Define dependency-ready sprint epochs, ordered tasklet packets, canonical
  orchestration, shared-checkout and isolated-worktree ownership, distributed
  evidence, validation reuse, and resource isolation.
- Focused proof: `node --test tests/plan-execution-subagents.test.js`.

### [DONE] Tasklet S01-F01-T04: Publish V3 contract ownership

- Advance both orchestration contract families to V3 and prove all immutable
  readers remain registered.
- Focused proof: `node --test tests/plan-execution-subagents.test.js`.

### [DONE] Tasklet S01-F01-T05: Synchronize generated host copies

- Regenerate the OpenClaw skill from the canonical source and verify equality.
- Focused proof: `node scripts/build-openclaw-skills.js` and
  `node --test tests/openclaw-skills.test.js`.

### [DONE] Tasklet S01-F01-T06: Validate and install Ponytail

- Run focused selectors, complete repository acceptance, rule-copy and version
  checks, then run `./scripts/install.sh` and verify the installed
  `plan-execution` copy matches the canonical source.
- No product build applies.

### [DONE] Tasklet S01-F01-T07: Reconcile the feature

- Confirm every tasklet and validation result, close the sprint and plan, and
  commit the complete project change-set.

## Validation Evidence

- Completed: `2026-08-26T12:17:35-07:00`.
- Focused selector and policy tests passed (25 tests); generated OpenClaw skill,
  rule-copy, version, and whitespace checks passed.
- Full `npm test` passed: 249 core tests, installer checks, 23 Pi tests, and 4
  MCP tests.
- The complete installer copied the final V3 skill into Codex; canonical and
  installed skill and selector files compared equal.
