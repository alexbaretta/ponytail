# S01: Global component registry

Status: DONE

Questions: [RESOLVED] The user requires detected full package names and
deduplicated shorthand aliases in `~/.ponytail/config.json`. The smallest
strict V1 extension keeps project paths stable and adds a path-keyed component
map.

Atomicity review: complete. CLI and test changes share the configuration
contract and are serialized in one implementation batch.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["cli", "tests", "docs"]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Persist component identities globally

Status: DONE

### [DONE] Tasklet S01-F01-T01: Extend global configuration V1

Require a component map whose keys exactly match registered project paths and
whose values are sorted unique names.

### [DONE] Tasklet S01-F01-T02: Synchronize component commands

Make project registration and component mutations update the local metadata
and global registry through the existing lock. Validate exact agreement.

### [DONE] Tasklet S01-F01-T03: Derive detected shorthand aliases

For detected scoped JavaScript package names, register the final path segment
as a shorthand. Deduplicate all full and shorthand names and preserve sorted
stdout.

### [DONE] Tasklet S01-F01-T04: Add focused proof and documentation

Exercise the exact configuration shape, synchronization, aliases, and
deduplication, then document the global registry contract.

### [DONE] Tasklet S01-F01-T05: Complete final acceptance

Run build impact and the repository's final acceptance commands, reconcile
the plan, and commit the complete change-set.

## Implementation evidence

- The focused CLI and project-validation suites passed 22 tests.
- Build impact reported no affected or indeterminate TSTS targets.
- Detection retains full package names in committed project metadata and
  records full names plus deduplicated scoped-package shorthands globally.
- Final `npm test` passed 297 core, 23 Pi extension, 4 MCP, and 58 TSTS tests.
- Rule-copy and version checks passed; all seven version files remain at
  `4.8.4`.
- The live registry contains Ponytail's full component names and derived
  shorthands. The implementation is committed as `91de9ce`.
