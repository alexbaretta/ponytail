# S01: Project component registry

Status: DONE

Questions: [RESOLVED] User supplied the commands, supported initial languages,
monorepo package-name detection behavior, stdout requirement, and future
language boundary. Implementation choices are fixed in the plan contract.

Atomicity review: complete. The ordered tasklets share the metadata reader and
tests deliberately; dependencies serialize those edits.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": [
      "cli",
      "scripts",
      "tests",
      "docs",
      "skills"
    ]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Component metadata and discovery

Status: DONE

### [DONE] Tasklet S01-F01-T01: Extend project metadata V1

Extend the strict project metadata V1 reader with required `components` and add
one atomic V1 writer. The user explicitly rejected a V2 format and backward
compatibility for the earlier shape.

### [DONE] Tasklet S01-F01-T02: Add manual component commands

Implement idempotent registration, explicit absent-component failure on
unregistration, repository-local locking, exact argument validation, and CLI
help.

### [DONE] Tasklet S01-F01-T03: Detect JavaScript and TypeScript packages

Read indexed regular `package.json` working-tree files without descending into
submodules. Parse every file before writing, union valid package names with
existing components, and print the sorted detected set. Support the exact
`typescript`, `javascript`, and `auto` language values.

### [DONE] Tasklet S01-F01-T04: Include components in reference QA

Search every foreign project component using the existing literal,
case-insensitive, bounded tracked-file scan and exception contract.

### [DONE] Tasklet S01-F01-T05: Add focused behavioral proof

Exercise the physical readers and all component command success/failure paths
through real Git fixture repositories. Run only the focused Node test files at
this tasklet gate.

### [DONE] Tasklet S01-F01-T06: Synchronize project configuration and docs

Write Ponytail's own metadata as V1, move TSTS from synonyms to components,
update project structure and skill guidance, generate required skill copies,
and document command/output semantics. The generator also repairs two stale
OpenClaw plan-execution script copies to match their existing canonical source;
this is mechanical configuration synchronization discovered before the batch
was frozen.

### [DONE] Tasklet S01-F01-T07: Complete final acceptance

Run build impact after final relevant edits, the full repository unit-test
command once, rule-copy validation, and version validation. Reconcile the plan
and commit the complete project change-set.

## Implementation evidence

- The focused CLI, project validation, project skill, and OpenClaw skill suite
  passed 125 tests with no failures.
- Ponytail has no build target or build-impact configuration, as declared in
  `AGENTS.md`; no build is applicable.
- The component detector registered Ponytail's four named tracked package
  manifests while preserving the explicitly registered `TSTS` component.
- Final `npm test` passed 296 core, 23 Pi extension, 4 MCP, and 58 TSTS tests.
- The rule-copy and version checks passed; all seven version files remain at
  `4.8.4`.
- The implementation is committed as `07b83da`. The final tree retains only
  physical metadata V1, with required `components` and no compatibility path.
