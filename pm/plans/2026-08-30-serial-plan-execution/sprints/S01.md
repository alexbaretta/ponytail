# S01: Implement Serial Plan Execution

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["skills/plan-execution", "tests", ".openclaw/skills/plan-execution", "pm/plans/2026-08-30-serial-plan-execution"]
  },
  "execution": {
    "status": "IN_PROGRESS",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

- Status: `IN_PROGRESS`
- Objective: make plan execution strictly serial while retaining complete
  batch amortization and immutable metadata readers.
- Questions: none; the user selected serial execution and installation.

## Feature S01-F01: Serial orchestration

### [DONE] Tasklet S01-F01-T01: Select one ready sprint

- Change planning and execution selection to return only the first
  deterministic dependency-ready sprint for every supported physical version.
- Focused proof: `node --test tests/ready-sprints.test.js`.

### [DONE] Tasklet S01-F01-T02: Select one complete tasklet batch

- Preserve V1 scalar selection, restrict V2 to its highest-ranked ready
  tasklet, and restrict V3 to one bounded root-and-descendant batch.
- Focused proof: `node --test tests/ready-tasklets.test.js`.

### [DONE] Tasklet S01-F01-T03: Define serial plan execution

- Remove planning and implementation delegation, secondary-task, worktree,
  execution-wave, runtime-capacity, and multi-writer instructions.
- Require one executing agent to complete readiness review, implementation,
  review, validation, lifecycle reconciliation, and commit before reselection.
- Focused proof: `node --test tests/plan-execution-serial.test.js tests/testing-cadence.test.js tests/agent-caused-repair.test.js`.

### [DONE] Tasklet S01-F01-T04: Synchronize published host copy

- Regenerate the OpenClaw skill from the canonical source and verify equality.
- Focused proof: `node scripts/build-openclaw-skills.js` and
  `node --test tests/openclaw-skills.test.js`.

### [DONE] Tasklet S01-F01-T05: Validate and install Ponytail

- Run focused tests, full repository acceptance, rule-copy and version checks,
  then `./scripts/install.sh`; compare installed and canonical plan-execution
  files byte-for-byte.

### [ ] Tasklet S01-F01-T06: Reconcile the change

- Record exact validation and installation evidence, close the sprint and
  plan, and commit only this plan-execution change-set.

## Validation Evidence

- `node --check skills/plan-execution/scripts/ready-sprints.js`: passed.
- `node --check skills/plan-execution/scripts/ready-tasklets.js`: passed.
- Focused plan-execution campaign: 38/38 tests passed.
- `node --test tests/openclaw-skills.test.js`: 92/92 tests passed.
- Skill Creator `quick_validate.py skills/plan-execution`: passed using the
  available Conda Python.
- Deprecated concurrent-execution language scan: no findings in
  `skills/plan-execution`.
- `git diff --check`: passed.
- `node scripts/check-rule-copies.js`: eight rule copies matched.
- `node scripts/check-versions.js`: all seven version files matched `4.8.4`.
- `npm test`: all 249 core tests, 23 Pi extension tests, and four MCP tests
  passed after the independent journal repair was committed as `44641da`.
- `./scripts/install.sh`: passed. The installed plan-execution skill matches
  the canonical directory byte-for-byte. The installer also synchronized the
  independently modified `cli/project_journal.sh`, as designed.

Tasklets T01-T05 are reconciled against the final validated tree and remain
provisional until their selected-batch commit succeeds. T06 owns final commit
evidence and plan closure.
