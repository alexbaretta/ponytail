# Sprint S03: Migrate Reusable Skills

Status: `PENDING`

## Objective

Move every enabled project-agnostic engineering and management skill
from `agent_harness` into Ponytail with preserved authorship, matching
criteria, and focused validation.

## Dependencies

- Sprint S02 is `DONE`.
- Decisions D03, D16, D19, D21 through D23.

## Questions

No open questions.

## Feature F01: Retire Obsolete Product Surfaces

### [ ] Tasklet S03-F01-T01: Remove Ponytail Gain

Remove the `ponytail-gain` skill, command, generated copies, runtime
registration, documentation, and tests. Keep benchmark evidence only in
the isolated benchmark subsystem.

### [ ] Tasklet S03-F01-T02: Keep Help As A Command

Remove `ponytail-help` from skill discovery and generated skill surfaces
while retaining one explicit help command backed by a canonical command
implementation.

### [ ] Tasklet S03-F01-T03: Adopt Technical Debt Syntax

Change debt harvesting and documentation from branded `ponytail:`
markers to exceptional `tech-debt:` markers, with `tech_debt.md` as the
preferred canonical record.

## Feature F02: Import Engineering Skills

Each tasklet imports one enabled skill, adds Alex Baretta's copyright and
MIT licensing notice, preserves project-agnostic matching criteria, and
adds focused validation metadata.

### [ ] Tasklet S03-F02-T01: Import API Service Boundaries

Import and validate `api-service-boundaries`.

### [ ] Tasklet S03-F02-T02: Import Parse-Safe Bash

Import and validate `bash-parse-safe-scripts`.

### [ ] Tasklet S03-F02-T03: Import Lossless JSON Contracts

Import and validate `lossless-json-contracts`.

### [ ] Tasklet S03-F02-T04: Import Monetary Contract Safety

Import and validate `monetary-contract-safety`, retaining the required
TypeScript `LosslessNumber` representation from `lossless-json`.

### [ ] Tasklet S03-F02-T05: Import React Error Containment

Import and validate `react-error-containment`.

### [ ] Tasklet S03-F02-T06: Import Static Type Safety

Import and validate `static-type-safety`, including exact owned-contract
fixtures and mechanically discriminated variants.

### [ ] Tasklet S03-F02-T07: Import Test Credentials

Import and validate `test-credentials` without imposing its directory
convention on projects that have not adopted it.

### [ ] Tasklet S03-F02-T08: Import Versioned Data Contracts

Import and validate the most recent complete
`versioned-data-contracts` skill.

## Feature F03: Import Management Skills

### [ ] Tasklet S03-F03-T01: Import Project Structure

Import and validate `project-structure`, including configuration-sync and
session-scoped immediate discrepancy repair.

### [ ] Tasklet S03-F03-T02: Import Plan Tasklets

Import and validate `plan-tasklets`, including plans, sprints, stories,
atomic tasklets, project change-sets, and standalone bug workflows.

## Sprint Validation

- Validate every imported skill's frontmatter, links, copyright, and
  project-agnostic content.
- Confirm `git-write-escalation` is disabled and unpublished.
- Confirm every retired skill and command is absent from published host
  surfaces.
