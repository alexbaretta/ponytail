# Sprint S03: Install Consistent Skill Snapshots

Status: `IN_PROGRESS`

## Objective

Ensure Codex sees Ponytail skills only after an explicit installation of a
consistent repository state. Installed bundled skills must be independent
copies, not live symlinks into the Ponytail working tree.

## Approval

The user reported the symlink installation bug, approved this correction, and
directed remediation on 2026-07-31.

## Feature F01: Snapshot Installation

### [DONE] Tasklet S03-F01-T01: Copy Bundled Skills

Replace each enabled bundled skill with a copied directory prepared before it
becomes visible at its installed path. Preserve unrelated user-owned skills
and reject paths not owned by this installer.

Validation:

- an installed bundled skill is a directory and not a symlink;
- changing its repository source after installation does not change the
  installed copy; and
- a subsequent explicit installation refreshes the copy.

### [DONE] Tasklet S03-F01-T02: Detect Installation Drift

Make installer check mode reject symlinks, missing copies, and installed
content that differs from the repository source.

Validation: focused installer tests cover symlink, source-edit isolation, and
content-drift failures.

### [DONE] Tasklet S03-F01-T03: Preserve Installer Boundaries

Keep global instructions and unrelated project-local Codex skill discovery
under their existing ownership rules. Continue removing obsolete
installer-owned entries without deleting unrelated files.

Validation: the complete installer suite passes and Bash syntax validation
passes.

## Feature F02: Remediate Existing Installation

### [TODO] Tasklet S03-F02-T01: Remove Live Project Symlinks

Enumerate symlinks directly under `~/.codex/skills`, resolve their targets,
and remove only those whose targets are inside this Ponytail repository.

Validation: no symlink under `~/.codex/skills` targets this repository, and no
target content is deleted.

### [TODO] Tasklet S03-F02-T02: Reinstall And Verify

Run the canonical installer from the committed correction, then run its check
mode.

Validation: every enabled bundled skill is an independent matching copy and
the build-impact skill contains the committed tooling.

## Sprint Validation

- Focused installer tests and `bash -n` pass.
- Ponytail final acceptance passes.
- The correction is committed without unrelated working-tree changes.
- The remediated Codex installation contains no symlink to this repository.

## Implementation Evidence

The installer stages each copied skill under the destination filesystem,
replaces the prior installer-owned path only after copying succeeds, and
restores the prior path if final placement fails. Check mode compares each
installed directory with its source and rejects both symlinks and drift.

`bash -n scripts/install-to-codex.sh scripts/test-install-to-codex.sh` and the
focused installer suite passed for the isolated correction and for the current
working-tree integration. The tests prove independent files, symlink
rejection, installed-content drift detection, refresh on reinstall, stale
entry cleanup, and preservation of unowned paths.
