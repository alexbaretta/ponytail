# S01: Implement the Ponytail CLI Lifecycle

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": ["cli", "scripts", "tests", "docs", "skills/codex-execpolicy", ".openclaw/skills/codex-execpolicy", ".ponytail", "AGENTS.md", "README.md", "PROJECT_STRUCTURE.md", "versioned-data-contracts.json", "package.json", "pm/plans/2026-09-04-ponytail-cli-lifecycle"]
  },
  "execution": {
    "status": "IN_PROGRESS",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

- Status: `IN_PROGRESS`
- Objective: make one installable CLI own explicit project registration and
  all Ponytail installation workflows.
- Questions: none. `ponytail install` combines the three installation commands
  and deliberately does not imply `setup-project`.

## Feature S01-F01: Explicit registration and installation

### [DONE] Tasklet S01-F01-T01: Define durable CLI configuration

- Add an exact V1 user configuration owning `sourceRoot` and sorted unique
  registered Git roots under `~/.ponytail`.
- Implement upward Git-root resolution and idempotent atomic registration.
- Focused proof: CLI registration tests.

### [DONE] Tasklet S01-F01-T02: Implement installation subcommands

- Make `install-permissions` load every registered project and invoke the
  existing proposal/acceptance compiler once.
- Delegate CLI and Codex installation through their existing safety-checked
  implementations and combine the three operations in `install`.
- Focused proof: isolated subcommand and combined lifecycle tests.

### [DONE] Tasklet S01-F01-T03: Synchronize command ownership

- Install the extensionless `ponytail` executable, delegate compatibility
  entrypoints, and update structure, package, skill, and user documentation.
- Regenerate the published skill copy.
- Focused proof: installer, distribution, and generated-skill checks.

### [ ] Tasklet S01-F01-T04: Validate and reconcile

- Run final repository acceptance, record evidence, and reconcile this plan
  without mutating the live user home.

## Validation Evidence

- `node --test tests/ponytail-cli.test.js
  tests/condense-codex-rules.test.js tests/cli-tools.test.js
  tests/distribution.test.js tests/openclaw-skills.test.js` passed 131 tests.
- The registered-project tests cover nested-root discovery, idempotence,
  concurrent registration, exact registry replacement, removal, and
  idempotent policy installation in isolated homes.
- Installed Codex parser validation passed all 11 permission and unified-CLI
  tests.
- Final `npm test` passed 264 core, 23 Pi extension, and 4 MCP tests.
