# Sprint S01: Installable Statistics CLI

- Status: `DONE`
- Objective: Implement, install, and validate the first Ponytail CLI tools.
- Dependencies: Explicit user approval of the plan.

## Questions

- No open questions. Output will use stable labeled lines, dates are inclusive,
  and plan names are exact directory basenames to preserve one lookup path.

## Feature F01: Statistics tools

Acceptance: Both tools locate the Git root, validate inputs, produce labeled
counts, and fail actionably outside their supported project structure.

### [DONE] Tasklet S01-F01-T01: Implement Plan Statistics

- Add the parse-safe `plan_stats.sh` tool and focused tests.
- Implemented exact-plan Git-root discovery and open/done line counts.
- Validation: focused plan-statistics and syntax tests passed.

### [DONE] Tasklet S01-F01-T02: Implement Bug Statistics

- Add the parse-safe `bug_stats.sh` tool and focused date and lifecycle tests.
- Implemented inclusive lifecycle counts with dashed and compact dates.
- Validation: focused bug-statistics and syntax tests passed.

## Feature F02: CLI installation

Acceptance: The installer installs all or selected known tools, supports a
configurable bin directory for testing, and manages only its explicit Bash
PATH line after confirmation or `--update-shell-path`.

### [DONE] Tasklet S01-F02-T01: Implement CLI Installer

- Add the parse-safe installer and focused installation, collision, selection,
  prompt, dry-run, check, and Bash PATH tests.
- Implemented all listed behaviors with an installer-owned manifest.
- Validation: all focused installer and syntax tests passed.

### [DONE] Tasklet S01-F02-T02: Synchronize Project Configuration

- Document CLI ownership and installation commands in the canonical project
  structure and agent configuration.
- Updated project structure, command inventory, package contents, distribution
  proof, and user documentation.

### [DONE] Tasklet S01-F02-T03: Validate And Commit The Plan

- Run Bash syntax checks and focused tests during implementation.
- Run final core acceptance, the rule-copy check, and the version check once
  after final edits.
- Inspect and commit the complete project change-set, then record validation.
- Full core acceptance, rule-copy validation, version validation, and package
  dry-run passed. The project change-set was committed as `dbc7ed4`.

## Validation Evidence

- Focused CLI suite: 9 tests passed.
- Full core acceptance: 206 tests passed.
- Codex installer, Pi, and MCP test families passed.
- Rule copies, repeated manifests, registry data, command adapters, runtime
  registry, versions, and package contents validated successfully.
