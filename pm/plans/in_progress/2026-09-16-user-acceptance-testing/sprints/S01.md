# S01: Define and publish user acceptance testing policy

Status: READY_FOR_REVIEW

Questions: [RESOLVED] See the manifest for scope and authority.

Atomicity review: complete. Each implementation tasklet owns one reusable
policy boundary or the publication synchronization that depends on those
boundaries. Ordered dependencies cover shared generated artifacts.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": [
      "skills",
      "tests",
      "registry.tsv",
      "generated",
      ".openclaw",
      "plugin.yaml",
      "README.md",
      "pm/plans"
    ]
  },
  "execution": {
    "status": "READY_FOR_REVIEW",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Portable UAT policy

Acceptance: sourced requirements remain distinct from observations; UAT traces
to approved requirements; persistent profiles operate only on test-owned state;
all concrete commands and tools remain project-local.

### [DONE] Tasklet S01-F01-T01: Define requirements provenance

Extend requirements with project-configurable source authority, claim-level
provenance, reconstruction status, and conflict handling.

### [DONE] Tasklet S01-F01-T02: Define persistent test profiles

Extend production-test boundaries with ephemeral and persistent non-production
profiles, ownership-safe fixture reconciliation, and preserved-state evidence.

### [DONE] Tasklet S01-F01-T03: Define user acceptance testing

Add the reusable UAT skill and interface metadata. Require a project-local
operations skill for concrete commands, environments, credentials, and tools.

### [DONE] Tasklet S01-F01-T04: Integrate project structure

Configure the UAT root as project-owned content and document the reusable skill
without claiming that Ponytail itself owns UAT records.

### [DONE] Tasklet S01-F01-T05: Publish and validate

Register the skill, generate every configured host surface, run focused and
full validation, install Ponytail, and reconcile this plan.

## Implementation evidence

- Requirements now distinguish approved, proposed, and observed statements;
  retain claim-level provenance; and stop on unresolved intended-behavior
  conflicts.
- Automated-test policy defines one Arc across ephemeral and persistent
  profiles and restricts persistent mutation to proven test-owned state.
- The reusable UAT skill delegates all concrete operations to a configured
  project-local skill and keeps release evidence distinct from approval.
- Project structure and the README recognize the configured UAT root without
  claiming that Ponytail itself owns UAT records.
- Seven focused existing policy, metadata, and configuration tests passed.
  `git diff --check` passed. The Skill Creator validator was unavailable
  because its runtime lacks the declared `yaml` module; repository-owned
  metadata validation remains assigned to the publication tasklet.
- The expanded focused selection passed 135 tests. Build impact reported no
  affected or indeterminate targets. Full `npm test` passed 327 core tests,
  installer checks, 23 Pi tests, 4 MCP tests, 58 TSTS tests, and the TSTS
  directory-structure gate.
- `scripts/install.sh` completed. The four installed changed skills compare
  byte-for-byte with source, including the new `user-acceptance-testing`
  skill, and the installed CLI points to this Ponytail checkout.

## Final acceptance

All feature behavior and publication surfaces are validated. Final lifecycle
markers remain provisional until the implementation commit succeeds.
