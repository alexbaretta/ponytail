# S01: Implement and accept the campaign census

Status: IN_PROGRESS

Questions: [RESOLVED] See the plan manifest. No product, contract, security,
persistence, infrastructure, ownership, destructive-action, or acceptance
question remains open.

Atomicity review: complete. Each tasklet owns one contract reader, traversal or
rendering operation, dispatch boundary, focused proof, configuration surface,
policy synchronization, generated publication step, or validation gate.
Repeated ownership of `src/campaign-census.js` is explicitly serialized.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": [
      ".agents/config/project",
      ".openclaw/skills/plan-execution",
      "cli",
      "src",
      "skills/plan-execution",
      "tests",
      "pm",
      "PROJECT_STRUCTURE.md",
      "README.md",
      "package.json",
      "versioned-data-contracts.json"
    ]
  },
  "execution": {
    "status": "IN_PROGRESS",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Campaign census contracts and CLI

Acceptance: the installed `ponytail` entrypoint validates and reports only the
campaign containing the supplied plan, through exact versioned contracts and
the canonical sprint/tasklet validators, without mutation.

### [DONE] Tasklet S01-F01-T01: Define the management configuration reader

Create V1 project-management configuration and its exact reader. Normalize the
management root, plan root, ordered lifecycle directories and roles, and legacy
flat-layout flag. Reject unknown keys, unsafe paths, duplicate directories, and
non-bijective lifecycle roles.

Focused proof: exact valid, malformed, unknown-key, and unsafe-path fixtures.

### [DONE] Tasklet S01-F01-T02: Define plan metadata and campaign discovery

Parse exactly one `ponytail-plan-campaign` block for selected members, follow
parent backlinks, derive descendants through deterministic scanning, and fully
validate only the resulting campaign. Unrelated malformed campaigns remain out
of scope.

Focused proof: root/leaf equivalence, missing and ambiguous parents, cycles,
duplicate member IDs, unrelated invalid campaigns, and legacy records.

### [DONE] Tasklet S01-F01-T03: Validate and normalize the census

Invoke canonical sprint and tasklet readers/selectors for every member,
normalize plan/sprint/tasklet records, compute exact counts and the zero-tasklet
case, and enforce member/root closure invariants.

Focused proof: valid mixed status census, selector failure, closure failure,
and zero-tasklet percentage.

### [DONE] Tasklet S01-F01-T04: Render human and versioned JSON output

Implement deterministic report ordering, the latest-only V1 JSON writer and
reader, stable fail-fast diagnostics, and exit `0`/`1`/`2` stream behavior.

Focused proof: human/JSON equivalence, exact newline and stream behavior,
unknown output keys, and repository identity fields.

### [DONE] Tasklet S01-F01-T05: Dispatch through the Ponytail CLI

Add the `campaign validate` and `campaign report` dispatch after ordinary
project validation without expanding the embedded Python implementation.

Focused proof: real `cli/ponytail` success, invalid-data, and usage paths.

### [DONE] Tasklet S01-F01-T06: Add focused campaign fixtures and tests

Build test-owned temporary Git repositories and exact plan/sprint/tasklet
fixtures that exercise the production module and CLI without adding a test-only
product path.

Focused proof: `node --test tests/campaign-census.test.js`.

### [DONE] Tasklet S01-F01-T07: Synchronize package and structure ownership

Ship `src/`, register the three durable representation families, document the
new source and configuration owners, classify them in the directory manifest,
and prove package inclusion.

Focused proof: distribution, package, versioned-contract, and directory
structure checks selected for the changed files.

### [DONE] Tasklet S01-F01-T08: Update reusable execution policy

Teach `plan-execution` to maintain campaign metadata and run scoped validation
at approval, pre-edit, relationship-change, and root-closure gates. Preserve
issue-tracking's separate epic meaning and the feature's census-only scope.

Focused proof: policy conformance assertions in
`tests/plan-execution-serial.test.js`.

### [DONE] Tasklet S01-F01-T09: Regenerate the published policy copy

Regenerate the OpenClaw `plan-execution` copy from its canonical source without
hand-editing generated content.

Focused proof: generated-copy and rule-copy checks.

### [ ] Tasklet S01-F01-T10: Validate and reconcile the feature

Review the complete change, run consolidated focused checks and the build-impact
query, run affected builds, prove the UAT Arcs at the production boundary, and
record implementation evidence before the implementation commit.

## Sprint validation evidence

Tasklets T01-T09 passed their consolidated focused gate on 2026-09-24:

- `node --test tests/campaign-census.test.js tests/plan-execution-serial.test.js tests/distribution.test.js tests/imported-skills.test.js tsts/dist/test/versioned-data-contracts.test.js`
  passed 23 tests with zero failures, skips, or todos.
- `bash -n cli/ponytail` passed, and the real
  `./cli/ponytail campaign validate` path validated this plan as a one-plan,
  one-sprint, ten-tasklet campaign.
- `node scripts/check-rule-copies.js` reported all eight rule copies current;
  `node scripts/check-versions.js` reported all seven version files pinned at
  `4.8.4`; `git diff --check` passed.
- The final build-impact query selected only `tsts` because `package.json` is
  a global input. `npm run build:tsts` passed after the final package input
  edit; later edits did not change a TSTS build input.
- The complete selected change-set was reviewed with no unresolved finding.

These tasklet markers become durable when the implementation batch commit
succeeds. Tasklet T10 remains the independent final feature gate.
