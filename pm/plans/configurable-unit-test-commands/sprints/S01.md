# Sprint S01: Define Portable Unit-Test Cadence

Status: `DONE`

## Objective

Define and verify the reusable configuration contract and execution cadence
before changing consumer repositories.

## Dependencies

- The plan is explicitly approved.
- All questions remain resolved.

## Questions

No open sprint-level questions.

## Feature F01: Define The Host Contract

### [DONE] Tasklet S01-F01-T01: Configure Named Unit-Test Families

Update `project-structure` so host instructions provide one named table of
focused and full unit-test commands. Permit multiple languages and runners,
and define `not applicable` and `not configured` behavior.

Validation: configuration-contract tests require the canonical generic keys
and reject hardcoded runner assumptions.

### [DONE] Tasklet S01-F01-T02: Bind Specialized Testing Skills

Make `production-test-boundaries` and `typescript-unit-testing` consume the
same generic family registry. Remove TypeScript-specific command ownership
while preserving TypeScript fixture, double, index, and metadata rules.

Validation: specialized-skill tests prove one command source of truth and
focused selection without fallback to a full command.

## Feature F02: Enforce Planned-Work Cadence

### [DONE] Tasklet S01-F02-T01: Strengthen Always-On Policy

Add concise project-neutral language to `skills/ponytail/SKILL.md`: planned
work uses only the smallest explicit focused unit-test selections until final
plan acceptance.

Validation: every compaction level contains the rule.

### [DONE] Tasklet S01-F02-T02: Correct Plan Testing Ownership

Remove feature/story permission to run an affected package's complete unit
suite. Require focused selections at tasklet, feature, and sprint levels, and
require all applicable configured full commands once at plan final
acceptance.

Validation: plan-execution tests cover every management level, missing focused
configuration, reuse of existing focused proof, and the final full-suite gate.

### [DONE] Tasklet S01-F02-T03: Define Standalone Cadence

Apply focused selection to direct changes and standalone bugs. Preserve full
commands only for explicit user requests and named host acceptance gates.

Validation: portable instructions cannot infer a full-suite run merely from
completion of ordinary work.

## Feature F03: Synchronize And Prove Ponytail

### [DONE] Tasklet S01-F03-T01: Generate Portable Policy Copies

Regenerate all host policy copies from the canonical Ponytail skill. Do not
hand-edit generated copies.

Validation: the rule-copy check passes.

### [DONE] Tasklet S01-F03-T02: Configure Ponytail's Test Families

Replace Ponytail's ambiguous unit-test entries with exact named focused and
full command forms discovered from the repository's existing test runners.
Keep final core acceptance separate from focused editing commands.

Validation: every configured focused form selects individual files or named
cases, and every full command names its owning final gate.

### [DONE] Tasklet S01-F03-T03: Run Focused Skill Tests

Run only the individual policy, plan cadence, project configuration, and
language-skill test files changed by this sprint. Query build impact for the
intended paths and follow its result.

Validation: all selected tests pass without invoking Ponytail's full unit-test
command.

### [DONE] Tasklet S01-F03-T04: Commit Portable Support

Inspect and commit only the portable policy, skills, generated copies, tests,
Ponytail host configuration, and sprint record.

Validation: the commit contains no consumer configuration or unrelated
working-tree changes.

## Sprint Validation

- Every feature and tasklet is `DONE`.
- Portable policy and specialized skills have one command registry.
- Focused checks pass without a broad unit-test run.
- Generated policy copies match their canonical source.
- Sprint S02 is ready with no unresolved command-contract questions.

## Completion Evidence

Ponytail now owns one named Node family with an explicit file-or-case focused
form and the repository's existing full command. Portable policy defines
multiple named families without a default, prohibits broad unit-test
selections before plan acceptance, and requires each configured full command
once at that final gate. The TypeScript skill consumes the shared family
registry rather than owning command aliases.

`node --test tests/policy-conformance.test.js
tests/project-skill-configuration.test.js tests/testing-cadence.test.js
tests/typescript-unit-testing.test.js` passed 15 focused tests. The rule-copy
generator and check passed for all eight generated copies. Ponytail has no
build target, so no build was applicable.
