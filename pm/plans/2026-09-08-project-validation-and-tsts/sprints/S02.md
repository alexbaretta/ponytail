# S02: Installable TSTS

Status: DONE

Questions: [RESOLVED] User explicitly authorizes MIT re-release and copyright Alex Baretta <alex@baretta.com>. Import source commit 23dfee0d97caaaa453c071c6dd6ce50c5e8c1914 (current clean checkout, newer than initial audit); read exact Git objects for a stable import. No downstream names found in current source/tests/docs. No source checkout or downstream submodule edits.

Atomicity review complete. Import units preserve upstream semantics; licensing is a deterministic header transformation. Mechanical packaging and documentation records are kept together because finer planning costs exceed direct edits.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S02",
  "planning": {
    "status": "APPROVED",
    "depends_on": [
      "S01"
    ],
    "scope_roots": [
      "tsts",
      "cli",
      "scripts",
      "tests"
    ]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [
      "S01"
    ],
    "tasklets_reviewed": true
  }
}
-->

## Feature S02-F01: Canonical installed analyzer
Status: DONE
Acceptance: installed CLI proves actual compiler behavior; runtime source has one repository owner and no downstream-specific references.

### [DONE] Tasklet S02-F01-T01: Import canonical analyzer module

Import the complete existing analyzer module without semantic edits. It exceeds 1000 lines because preserving the source-proven module is one import operation; refactoring would broaden scope.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T02: Import analyzer CLI

Import CLI argument parsing and analyzer dispatch without changing analyzer semantics.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T03: Import lossless JSON boundary

Import existing serialization boundary; replace proprietary notice with authorized MIT copyright.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T04: Import regression fixture corpus

Import the upstream regression corpus unchanged except authorized copyright notices. This is one preserved test-support import, not newly designed test declarations.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T05: Configure compiled runtime

Common package ownership at root, ESM boundary under tsts. TypeScript output ignored; explicit build-impact target includes root dependencies and source/tests. No copied lockfile or separate dependency tree.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T06: Installable launcher

Parse-safe main resolves its canonical symlink source, calls sibling ponytail validate, then executes compiled analyzer with user args and exact child status. No runtime install/build or fallback.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T07: Installer integration

Include tsts alongside ponytail as a source-linked executable with manifest ownership checks. No outside installation in this task.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T08: Package lifecycle

Add runtime TypeScript/lossless-json, dev node types, npm bins, compiled-runtime files and build/test commands. prepare compiles source installs; distribution excludes test artifacts. Core tests include TSTS tests without repeated builds.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T09: Installed behavior proof

Real isolated CLI install and registration; passing/failing typed projects through installed analyzer. Package test ensures runtime ships and test fixtures do not. Extend parse-safe executable inventory.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T10: Documentation and contracts

Document provenance revision and MIT authorization; canonical build, focused/full TSTS tests and metadata ownership. Record no analyzer semantics changes and no downstream submodule removal. Add config/manifest durable contracts to inventory.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

### [DONE] Tasklet S02-F01-T11: Final plan acceptance

Run build-impact after final source/config edit, build once, focused installed-CLI/packaging proof, full npm test once, rule-copy/version/generator guards. Reconcile all tasklets and commit.

Focused gate: `node --test tests/tsts-cli.test.js tests/distribution.test.js tests/cli-tools.test.js` after consolidated build; compiler proves imported source/fixtures. Full inherited TSTS tests belong to final plan acceptance. Exclude runtime test-only mechanisms, unrelated analyzer fixes, upstream writes, and CI/CD changes.

Batch evidence: configured build-impact returned affected target tsts with no indeterminate targets; npm run build:tsts passed. All 25 focused tests across tsts-cli, distribution and cli-tools passed, including real installed CLI success/failure and npm runtime/test packaging boundary. All 12 source/test imports verified byte-identical after removal of authorized header differences. Full inherited TSTS tests and full root acceptance owned by T11.

### [DONE] Tasklet S02-F01-T12: Freeze Git grep scope and format

Final source review found Git user configuration can enable submodule recursion or output decorations. In scripts/project-qa.js checkReferences, explicitly disable recursion, textconv, color, columns, heading and breaks. Preserve tracked working-tree scope. tests/project-validation.test.js adds a real submodule fixture with foreign text only inside it and configured grep decorations; assert no submodule findings and precise parent-file findings. No semantic expansion. Focused validation: node --test tests/project-validation.test.js. S02 depends on S01 and owns this ordered correction to its earlier checker path. Full final acceptance reruns after this relevant input change.

T12 evidence: focused regression failed before correction (configured grep columns/color caused a false clean result for a parent-file reference). After explicit output/scope options, all 9 project-validation tests passed. Build-impact reported no affected or indeterminate target; reused TSTS build. Initial full acceptance before this correction passed 374 tests and installer checks; final acceptance must run against corrected tree.

Final acceptance against 7ced1dc: npm test passed (290 core + 23 extension + 4 MCP + 58 TSTS = 375 tests; no failures/skips), plus Codex installer checks. Rule-copy check: 8 match; version check: 7 pinned at 4.8.4. Registry, generated runtime, command-adapter, and manifest checks passed inside npm test. Compiled TSTS input unchanged since successful selected build. No downstream integration suite or outside installation performed. Source worktree clean before this documentation-only reconciliation.
