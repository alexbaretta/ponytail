# S01: Project lifecycle and reference QA

Status: DONE

Questions: [RESOLVED] User authorizes implementation. TSTS licensing belongs to S02 and does not block S01. No package-manager commands or expensive integration workflows are executed by QA. Exact-file exceptions use explicit identities; no broad wildcard suppression. User global config is untouched by implementation; test fixtures use isolated homes.

Atomicity review: complete. Ordered shared paths below. Tests are dedicated behavioral fixture units; documentation sync recorded as mechanical ownership exception.

<!-- ponytail-plan-sprint
{
  "schemaVersion": 3,
  "id": "S01",
  "planning": {
    "status": "APPROVED",
    "depends_on": [],
    "scope_roots": [
      "cli",
      "scripts",
      "tests",
      "docs",
      "skills",
      ".openclaw",
      ".ponytail"
    ]
  },
  "execution": {
    "status": "DONE",
    "depends_on": [],
    "tasklets_reviewed": true
  }
}
-->

## Feature S01-F01: Registered local development
Status: DONE
Acceptance: plan lifecycle and reference contracts proved through real CLI fixture invocations.

### [DONE] Tasklet S01-F01-T01: Git identity and registration

repository_roots and register: resolve Git common directory/worktree porcelain; initialize main metadata and policy; normalize historical registered worktrees without changing global V1 shape. Retain existing registration lock and idempotent writes.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T02: Project metadata reader

read_project: strict V1 metadata with canonical name, names, repositoryUrls, packages, manifests, exceptions. Reject unknown keys and malformed relative paths. JSON registry boundary is inventoried.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T03: Cheap validation

Canonical policy parser gains a read-only --validate-project mode in cli/condense_codex_rules.sh; no accepted-policy writes. validate_project: reject absent registration with exit 3 and missing/invalid project or policy metadata with exit 1; resolve current worktree metadata. No scans, package manager, or tests.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T04: CLI dispatch

run: require Git first (exit 2); register sole validation exception, including help. Replace setup command. validate prints success; qa invokes Node checker with validated inventory. Release global lock before QA/updates.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T05: Dependency declarations

declared_dependencies: strict npm/pnpm JSON, Cargo TOML, Python pyproject TOML/requirements, Brewfile and Conda environment YAML readers; only direct names. Read explicit tracked manifests; no network or package installation. Parse with yaml and smol-toml dependencies.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T06: Submodule permission

allowed_projects: validate .gitmodules paths against Git index mode 160000 and match declared URLs to project coordinates, including relative URL resolution. Dependencies match exact manager/name coordinates; npm and pnpm share npm identity.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T07: Tracked reference scan

check_references: git grep literal case-insensitive names with word boundaries, working-tree tracked text, no recursive submodules; return file/line/project/name diagnostics. Exact file/name/project exceptions require reasons; stale exceptions report warnings. Exception metadata values are allowed only in their own exception records to avoid self-reference.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T08: Behavioral proof

Node fixtures exercise real CLI and Git repositories; no unregistered external state or product test hooks. Cover errors, worktrees, QA split, package readers, submodules, tracked/untracked inputs, exceptions, malformed manifests.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T09: Lifecycle regression adaptation

Update existing setup tests to register and explicitly register fixture repos for all commands. Preserve concurrency, permissions, installation proof.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T10: Documentation and distribution synchronization

Document versioned metadata, exact dependency formats, exit codes, coverage, local-only QA. Update structure, commands, contract inventory, own metadata, skills and generated copies. These are mechanical synchronization of the selected implementation; finer tasklets would cost more than the direct edit.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

### [DONE] Tasklet S01-F01-T11: Acceptance gate

Run focused Node files and Bash syntax, generator/registry checks. Full npm test at final plan gate; core has no build targets until S02. No outside mutations.

Focused proof: `node --test tests/project-validation.test.js tests/ponytail-cli.test.js` at consolidated batch gate; no builds. Exclusions: other repository writes, installed/home configuration, CI/CD and integration suites.

Batch evidence: 125 focused Node tests passed across project-validation, ponytail-cli, condense-codex-rules, project-skill-configuration, and openclaw-skills. Bash syntax and git diff --check passed. Eight rule copies and seven versions passed. No build target affected. Node parser dependencies installed into this repository; initial restricted-network download failed, approved retry succeeded. Full acceptance owned by S02 final plan gate.

S01 acceptance: reuse unchanged passing combined-behavior evidence from batch 03e9579; no separate integration suite exists. All feature tasklets accepted.
